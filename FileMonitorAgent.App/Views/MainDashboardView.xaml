<UserControl
    x:Class="FileMonitorAgent.App.Views.MainDashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> 
            <RowDefinition Height="Auto" /> 
            <RowDefinition Height="*" />    
            <RowDefinition Height="Auto" /> 
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="15"
            Background="#F8F9FA"
            BorderBrush="#DEE2E6"
            BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel>
                    <TextBlock
                        x:Name="ProfileNameText"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#333"
                        Text="Profil: Nazwa" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Green"
                        Text="Status: Monitorowanie aktywne" />
                </StackPanel>
                <Button
                    Padding="15,5"
                    VerticalAlignment="Center"
                    Background="#DC3545"
                    Click="BtnLogout_Click"
                    Content="Wyloguj"
                    DockPanel.Dock="Right"
                    FontWeight="Bold"
                    Foreground="White" />
            </DockPanel>
        </Border>

        <Expander
            Grid.Row="1"
            Margin="10,5"
            Foreground="DarkSlateGray"
            Header="Podgląd monitorowanych folderów">
            <ListBox
                x:Name="WatchedFoldersList"
                MaxHeight="100"
                Background="#F1F1F1"
                BorderThickness="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Margin="5"
                            Foreground="DimGray"
                            Text="{Binding Path}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Expander>

        <ListView
            x:Name="LogsList"
            Grid.Row="2"
            Margin="10,5"
            BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            Foreground="Gray"
                            Text="{Binding Time}" />

                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                            <TextBlock Margin="0,0,5,0" Text="{Binding Icon}" />
                            <TextBlock FontWeight="Medium" Text="{Binding Message}" />
                        </StackPanel>

                        <Button
                            Grid.Column="2"
                            Margin="10,0,0,0"
                            Padding="8,2"
                            Background="#E2E6EA"
                            Click="BtnOpenFolder_Click"
                            Content="Otwórz folder"
                            Cursor="Hand"
                            Tag="{Binding FolderPath}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StatusBar
            Grid.Row="3"
            Background="#007ACC"
            Foreground="White">
            <StatusBarItem>
                <TextBlock x:Name="PathsCountText" Text="Monitorowane foldery: 0" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
