pub fn send_new_file_toast(_file_path: &str, _title: &str, _custom_msg: Option<&str>) {
    // Nie używamy już natywnych Toastów Windows, okno eframe przejęło tę funkcję
    // Zostawiam pustą funkcję, żeby nie musieć przerabiać całego kodu watcher.rs
}